# These feature files are auto-generated by a tool, and can only be CRLF.
# With this set, we believe that every *other* file in the repo should be LF,
# and that you should be able to work with autocrlf set to `input`.
*.feature.cs text eol=crlf